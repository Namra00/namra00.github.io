# Deployment fix

This version uses new stylesheet and script filenames to avoid GitHub Pages or browser caches serving the previous portfolio CSS.

Copy every file and folder from this package into the repository root, commit, and push. The `index.html` file must be beside the `assets`, `images`, and `documents` folders.

After GitHub Pages finishes deploying, open the site in a private/incognito window or hard-refresh once.
